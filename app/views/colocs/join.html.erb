<div class="bnr">
  <div class="logo-banner">
    <%= image_tag 'logo.png', class:'w-52' %>
  </div>
  <%= cl_image_tag current_user.avatar.key, class:'avatar avatar-md absolute top-1 right-1' %>
  <div class="banner-divider">
    <%= image_tag 'divider.svg' %>
    <%= image_tag 'divider.svg' %>
  </div>
</div>

<h1 class="text-center mb-2 upcase text-3xl text-primary-200 font-medium uppercase">Rejoins tes colocs</h1>

<h3 class="text-center mb-8 text-medium">Entre le code de la coloc :</h3>

<div class="text-center">
    <%= simple_form_for @user do |f| %>
        <div>
            <input type="text", value="", name="invite_token", class="invit-token">
        </div>
        <div>
            <%= f.submit "Je rejoins", class: 'btn-primary' %>
        </div>
    <% end %>
  
    <p class="mt-8 text-sm lg:text-base">
        <%= link_to "Retour à l'écran d'accueil",  home_path, class:"btn-border-purple"%>
    </p> 
</div>
