<h1 class="text-center mb-2 upcase font-default text-3xl text-primary-200 font-medium uppercase">Rejoins tes colocs</h1>

<h3 class="text-center mb-8 font-default text-medium">Entre le code de la coloc :</h3>

<div class="text-center">
    <%= simple_form_for @user do |f| %>
        <div>
            <input type="text", value="", name="invite_token", class="invit-token">
        </div>
        <div>
            <%= f.submit "Je rejoins", class: 'btn-primary' %>
        </div>
    <% end %>
</div>
